---
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "Updates";

// Optional: put the push meta in a comment block if you still want the workflow to parse it
/**
 * PUSH_META
 * title: New update
 * message: A fresh update is live -- tap to read.
 * PUSH_META_END
 */
---

<BaseLayout title={pageTitle}>
  <!-- PAGE HEADER SECTION -->
  <section class="updates-hero">
    <header class="page-header">
      <h1>Updates</h1>
      <p class="page-subtitle">
        Announcements, changes, and little milestones as Lystaria grows.
      </p>
    </header>
  </section>

  <!-- OPTIONAL DIVIDER (keep or remove later) -->
  <GradientDivider />

  <!-- LATEST UPDATE -->
  <section class="updates-entry">
    <h2 class="updates-title">Latest</h2>

    <p class="updates-body">Latest Update: Doing a site overhaul because I dont want pages to look so narrow on mobile and posting another Zodiac in the zodiac series today. Stay tuned.</p>

    <div class="meta-row">
      <span class="meta-label">Published</span>
      <span class="meta-value">January 6, 2026</span>
    </div>
  </section>

  <!-- PREVIOUS / ADDITIONAL UPDATE -->
  <section class="updates-entry">
    <p class="updates-body">
      Hey my lovely subscribers! I now have push notifications enabled. To add them,
      you need to go to your browser and hit the share button on my homepage, then hit
      "Add to Home Screen." Open the app you just added, tap the blue bell, and enable
      push notifications for all updates and posts. You’ll be notified each time I
      publish one or the other. Thank you for being here -- I love you!
    </p>

    <div class="meta-row">
      <span class="meta-label">Published</span>
      <span class="meta-value">January 6, 2026</span>
    </div>
  </section>

<style>
  .page-header {
    max-width: 900px;
    margin: 0 auto 1.5rem;
    text-align: center;
  }

  .page-subtitle {
    max-width: 900px;
    margin: 0.75rem auto 0;
    opacity: 0.9;
    line-height: 1.6;
  }

  .updates-title {
    margin: 0 0 0.75rem;
  }

  .updates-body {
    margin: 0 0 1rem;
    line-height: 1.7;
  }

  /* ✅ renamed to match your new markup */
  .updates-entry {
    max-width: 700px;
    margin: 3rem auto 0;
    padding: 2rem;
    border: 2px dotted transparent;
    border-image: var(--gradient-accent) 1;
    border-radius: 8px;
    font-size: 0.85rem;
    color: #ffffff;
  }

  .meta-row {
    display: flex;
    gap: 0.75rem;
    opacity: 0.9;
    font-size: 0.95rem;
  }

  .meta-label {
    opacity: 0.8;
  }

  /* ✅ these exist in your markup */
  .meta-value {
    opacity: 0.85;
  }

  /* =========================
     MOBILE
     ========================= */
  @media (max-width: 1100px) {
    .page-header {
      margin-bottom: 1.25rem;
    }

    .page-subtitle {
      font-size: 0.95rem;
    }

    .updates-entry {
      margin-top: 2rem;
      padding: 1.5rem;
      font-size: 0.85rem; /* keep readable */
    }

    .meta-row {
      font-size: 0.9rem;
      flex-wrap: wrap; /* prevents awkward squeezing */
    }
  }
</style>
</BaseLayout>