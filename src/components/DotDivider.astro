<div class="dot-divider">
  <span class="dot"></span>
  <span class="dot"></span>
  <span class="dot"></span>
  <span class="dot"></span>
</div>

<style>
  .dot-divider {
    display: flex;
    gap: 1rem;
    justify-content: center;
    align-items: center;
    margin: 3rem auto;
  }

  .dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  .dot:nth-child(1) {
    background: linear-gradient(135deg, #9a2dfe, #e019d4);
    animation-delay: 0s;
  }

  .dot:nth-child(2) {
    background: linear-gradient(135deg, #e019d4, #00dbff);
    animation-delay: 0.3s;
  }

  .dot:nth-child(3) {
    background: linear-gradient(135deg, #00dbff, #9a2dfe);
    animation-delay: 0.6s;
  }

  .dot:nth-child(4) {
    background: linear-gradient(135deg, #9a2dfe, #00dbff);
    animation-delay: 0.9s;
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 0.4;
      transform: scale(1);
    }
    50% {
      opacity: 1;
      transform: scale(1.3);
    }
  }
</style>
